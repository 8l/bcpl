// This is the 64-bit BCPL to Cintcode compiler

// Implemented by Martin Richards (c) May 2013

// Note: ../cintcode/ is used so that the compiler can be
// compiled in a directory parallel to cintcode (such as natbcpl).

// Note: the 64-bit BCPL compiler uses the standard front end.

GET "../cintcode/com/bcplfe.b"

.

GET "../cintcode/com/bcpl64cgcin.b"
